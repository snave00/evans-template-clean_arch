import 'package:flutter/material.dart';

class ActivityPageWrapper extends StatelessWidget {
  const ActivityPageWrapper({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Activity Page'),
      ),
    );
  }
}
